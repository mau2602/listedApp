    // receives the clicked button, extracts its category and stores and return it in a variable

    export default function category(button){
        let catBtn = button
        return catBtn
    
    }
// adding products to a list
import User from './model/usersSchema.js'

const listName = 'carrefour'

async function createItem(){

}

async function createList(list, ){

    const products = []

    const lists = User.findById('Find by user session ID')
}

